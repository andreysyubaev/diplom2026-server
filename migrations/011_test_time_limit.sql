-- Лимит времени на прохождение теста (в минутах).
-- NULL — без ограничения, как было раньше; иначе по истечении этого
-- срока клиент автоматически отправляет ответы на сервер,
-- неотвеченные вопросы засчитываются как неверные.

ALTER TABLE tests
    ADD COLUMN IF NOT EXISTS time_limit_minutes INTEGER;
